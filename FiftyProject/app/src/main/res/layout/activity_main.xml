<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="409dp"
        android:layout_height="729dp"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="1dp"
        tools:layout_editor_absoluteY="1dp"
        tools:ignore="MissingConstraints">


        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <TableRow>

                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar_menu"
                    android:layout_width="match_parent"
                    android:background="@color/white"
                    app:title="DANH SACH CONG DAN"
                    app:titleTextColor="@color/white" />
            </TableRow>
            <TableRow>

                <EditText
                    android:id="@+id/et_Hoten"
                    android:layout_width="397dp"
                    android:layout_height="match_parent"
                    android:hint="Nhap ho ten: " />
            </TableRow>
            <TableRow>
                <EditText
                    android:id="@+id/et_CCCD"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:hint="Nhap CCCD: " />
            </TableRow>
            <TableRow>
                <TableLayout>
                    <TableRow android:gravity="center">
                        <Button android:text="Them" android:id="@+id/btnAdd"/>
                        <Button android:text="Sua" android:id="@+id/btnEdit"/>
                        <Button android:text="Xoa" android:id="@+id/btnRemove"/>
                    </TableRow>
                </TableLayout>
            </TableRow>
            <TableRow>

                <ListView
                    android:id="@+id/lv"
                    android:layout_width="match_parent" />
            </TableRow>
        </TableLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>